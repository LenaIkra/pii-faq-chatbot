# PII FAQ Chatbot  
Виртуальный помощник магистратуры «Прикладной искусственный интеллект» (УрФУ + Нетология).

Проект реализует чат-бота, отвечающего на вопросы абитуриентов и действующих студентов.  
Используются эмбеддинги, TF-IDF, ранжирование, FAQ-кнопки, Telegram-интерфейс и CLI.

---

## 🎯 Цели проекта

- Автоматизировать ответы на частые вопросы студентов и абитуриентов.
- Снизить нагрузку на координаторов магистратуры.
- Дать пользователям удобный интерфейс: Telegram-бот, CLI, FAQ-меню.
- Включить элементы ML / NLP для улучшения точности поиска ответа.

---

## 🚀 Функциональность

### Основные возможности
- Два режима:
  - **Студент**
  - **Абитуриент**
- Два сценария работы:
  - **FAQ-меню** (готовые кнопки)
  - **Свободный вопрос** (поиск по базе)
- Гибридный поиск:
  - sentence-transformers (эмбеддинги)
  - TF-IDF для усиления семантики
  - ранжирование по гибридному скору
- Сбор обратной связи (👍/👎)
- Логирование запросов
- CLI-версия и Telegram-бот

---

## 🧱 Технологии

- Python 3.10+
- HuggingFace `sentence-transformers`
- Scikit-learn (TF-IDF)
- NumPy, pandas
- python-telegram-bot 21.x
- FastAPI (по желанию, для web API)

---

## 📦 Установка

```bash
git clone <URL_репозитория>
cd pii-faq-chatbot

python -m venv venv
source venv/bin/activate   # macOS/Linux
# .\venv\Scripts\activate  # Windows

pip install -r requirements.txt
````

---

## 📂 Структура проекта

```text
pii-faq-chatbot/
  src/
    build_kb.py          # Построение базы знаний из Excel
    chat_cli.py          # Консольный чат
    faq_bot.py           # Логика RAG + TF-IDF + эмбеддинги
    faq_config.py        # Конфигурации и константы
    logger.py            # Логирование действий
    stats_manager.py     # Учет фидбэка 
    telegram_bot.py      # Telegram-бот
    web_app.py           # Web API (опционально)
  data/
    faqs_raw.xlsx        # Исходная база вопросов (ручной ввод)
    faq_kb_students.csv
    faq_kb_applicants.csv
    faq_embeddings_students.npy
    faq_embeddings_applicants.npy
  logs/
    interactions.log      # История запросов
  requirements.txt
  README.md
  .gitignore
```

---

## 🛠 Подготовка данных

### 1. Положите исходный Excel:

`data/faqs_raw.xlsx`

Формат:

* колонки: `question`, `answer`, `segment`

Пример:

| question              | answer             | segment    |
| --------------------- | ------------------ | ---------- |
| когда защита вкр      | ВКР защищается ... | students   |
| какие документы нужны | ...                | applicants |

---

### 2. Постройте базу знаний:

```bash
python -m src.build_kb
```

Скрипт автоматически создаст:

* `data/faq_kb_students.csv`
* `data/faq_kb_applicants.csv`
* `data/faq_embeddings_students.npy`
* `data/faq_embeddings_applicants.npy`

---

## 💬 CLI-чат

Запуск:

```bash
python -m src.chat_cli
```

Возможности:

* выбор режима (студент/абитуриент)
* задавать вопросы текстом
* включить отладку через флаг:

```
debug = True
```

---

## 🤖 Telegram-бот

### 1. Создайте бота в Telegram через @BotFather и получите токен.

### 2. Экспортируйте токен:

```bash
export TELEGRAM_BOT_TOKEN="ваш_токен"
```

### 3. Запустите бота:

```bash
python -m src.telegram_bot
```

Функции:

* переключение режимов
* FAQ-меню (кнопки)
* свободный вопрос
* фидбэк (👍/👎)
* логирование всех взаимодействий

---

## 📊 Мониторинг и статистика

Хранится в:

```
logs/interactions.log
```

Фиксируется:

* сегмент (students/applicants)
* вопрос
* ответ
* confidence score
* фидбэк

---

## 🧪 Подходы и методы (для защиты)

Использовано:

* **Sentence-BERT multilingual MiniLM-L12-v2**
* **TF-IDF с cosine similarity**
* гибридное ранжирование (α * cosine_emb + β * tfidf_sim)
* fallback при низкой уверенности
* FAQ-matching через индексы
* сбор фидбэка как метрики качества

Почему так:

* эмбеддинги хорошо ловят семантику
* TF-IDF усиливает совпадения ключевых слов
* гибрид работает устойчивее для доменных вопросов

---

## 🧾 Ограничения

* Эмбеддинги нужно пересоздавать при обновлении базы.
* Ответы ограничены содержанием FAQ.
* Не заменяет живого координатора при сложных кейсах.

---

## ✔️ Критерии качества (для Нетологии)

Проект закрывает требования:

* бизнес-ценность — автоматизация FAQ
* ML-компонент — гибридный поиск + эмбеддинги + TF-IDF
* стабильность — работает CLI и Telegram
* воспроизводимость — build_kb + README + requirements
* метрики — логирование + фидбэк

---

## 📹 Демонстрация




